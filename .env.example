# Copy this file to .env and fill in the values from Google Cloud Console
# (see CLAUDE.md > Google Cloud setup for step-by-step instructions).
# Both values are safe to expose client-side once restricted as described there.

VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
VITE_GOOGLE_API_KEY=your-picker-api-key
