// The whole app talks to Google APIs from the browser and is deployed as a
// static bundle (see adapter-static fallback: 'index.html'), so server-side
// rendering is disabled everywhere.
export const ssr = false;
